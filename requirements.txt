# KDAA — Korean Dubeolsik Acoustic Attack
# Verified on: Python 3.11
torch>=2.1          # model/training (works on CPU; uses GPU automatically if present)
numpy>=1.24
scipy>=1.10         # WAV loading (scipy.io.wavfile), signal processing
scikit-learn>=1.3   # confusion_matrix, metrics
matplotlib>=3.7     # grayscale figure output
pandas>=2.0         # metadata.csv handling
pyyaml>=6.0         # config
# optional (used if present, works without):
# soundfile        # loading formats other than wav
# tqdm             # progress bar
